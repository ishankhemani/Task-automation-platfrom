# Frontend Environment Variables
# Copy this file to .env and update values for your environment

# Backend API base URL (relative for proxy / Nginx or absolute for cross-domain)
VITE_API_BASE_URL=/api/v1

# Socket.IO server URL (leave empty for same-origin proxy)
VITE_SOCKET_URL=

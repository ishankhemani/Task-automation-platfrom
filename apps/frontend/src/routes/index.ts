// Route path definitions & router setup placeholder

export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  TASKS: '/tasks',
  WORKFLOWS: '/workflows',
  WORKERS: '/workers',
  METRICS: '/metrics',
} as const;
